version: "3.9"
services:
  app:
    build: ./app
    command: streamlit run bot.py #--server.port 8501  --server.address "0.0.0.0"
    ports:
      - "8501:8501" 
    environment:
      - STREAMLIT_SERVER_HEADLESS=true

  # db:
